# Operator artwork

Place operator portraits and operator-specific artwork here.

- `badges/` — 75 supplied operator and recruit badge SVG files, preserved under
  their lowercase `<operator>.svg` filenames.
- Future portrait artwork should use lowercase, hyphenated filenames based on
  the operator name, for example `thatcher.png`.
