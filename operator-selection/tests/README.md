# Operator selection tests

This directory is reserved for layout and behavior checks, especially the
fixed 1280 x 720 canvas, five operators per side, 3 x 4 weapon-token matrices,
and Ban & Pick sequence states defined in `../docs/UI_LAYOUT_SPEC.md`.
