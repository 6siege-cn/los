# 发布约定

用户已要求本项目每次修改完成后自动发布，无需再次询问。

- 完成用户要求的修改并通过相关检查后，提交本次任务涉及的文件，推送到 `main`，触发现有 GitHub Pages 工作流。
- 确认部署成功并核验线上版本后，再向用户报告发布完成及访问链接。
- 检查或部署失败时说明实际状态，不得将未上线的版本称为已发布。
- 不发布未完成的中间修改，不包含其他任务尚未确认的改动，不关闭检查或覆盖他人的提交。
- 此约定是完成修改后的发布流程，不是定时任务或对文件变动的后台监控。
